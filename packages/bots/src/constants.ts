// The character that must be used for commands, e.g. "!today"
export const commandPrefix = "!"

// The maximum number of characters that can fit in a single YouTube chat
// message.
export const MAX_YOUTUBE_CHAT_MESSAGE_LENGTH = 200
